# Virtual environments
accredian-env/
env/
.venv/
*.env

# Jupyter checkpoints
.ipynb_checkpoints/

# Python caches
__pycache__/
*.pyc

# Data — download Fraud.csv from Kaggle, see README
*.csv
# ...but the small SQL aggregate outputs ARE tracked: they are the report's and the
# dashboard's only input, and the dashboard has no 471 MB CSV to recompute them from.
!app_data/*.csv

# Generated model artefacts
*.pkl

# Not part of this project
nexora.ipynb
resume_extract*.yaml

# Note: figures/ IS tracked — the README embeds those PNGs.

# Resume extraction material — personal career data, never committed
bullet_extract_latest.yaml
bullet_extract_[0-9]*.yaml

# SQLite build artefact — rebuild with `python load_sqlite.py`
fraud.db

# Generated spreadsheet report — rebuild with `python make_report.py`
reports/
